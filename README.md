# Mini DBMS Project

This project implements a simple DBMS in Java.

## Features
- CREATE, INSERT, SELECT, DELETE
- WHERE conditions
- BST indexing for primary keys
- Aggregates (COUNT, MIN, MAX, AVG)
- File persistence
- LET operation

## Run
javac *.java
java DBMS